<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />

    <application
        android:name=".SystemAppDaily"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.SystemAppDaily"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">

        <!-- Login Activity (Launcher) -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SystemAppDaily.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Home Activity -->
        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Lectura Activity -->
        <activity
            android:name=".ui.lectura.LecturaActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Camera Activity -->
        <activity
            android:name=".ui.lectura.CameraActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Main Activity (Navigation Drawer - Dashboard) -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Firma Activity (Configurar firma del revisor) -->
        <activity
            android:name=".ui.firma.FirmaActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Impresora Activity (Configurar impresora BT) -->
        <activity
            android:name=".ui.impresora.ImpresoraActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Revision Activity -->
        <activity
            android:name=".ui.revision.RevisionActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Acta Revision Activity (firmas e impresión) -->
        <activity
            android:name=".ui.revision.ActaRevisionActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Macromedidores Module -->
        <activity
            android:name=".ui.macro.MacromedidoresActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.macro.MacroLecturaActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Revisiones V2 Module -->
        <activity
            android:name=".ui.revision_v2.RevisionesActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.revision_v2.RevisionWizardActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- FileProvider para compartir fotos -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
